import 'package:flutter/material.dart';

class WorkCompleteComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      "Complete Work",
      style: TextStyle(
        fontSize: 20, 
        fontWeight: FontWeight.bold, 
      ),
    );
  }
}