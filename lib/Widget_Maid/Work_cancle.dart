import 'package:flutter/material.dart';

class WorkCancleComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      "Cancle Work",
      style: TextStyle(
        fontSize: 20,
        fontWeight: FontWeight.bold,
      ),
    );
  }
}
